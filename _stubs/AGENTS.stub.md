# AGENTS.md (local stub, not committed)

This project's persistent knowledge base ("brain") lives outside the repo:

    {{BRAIN_PATH}}

Project id: `{{PROJECT_ID}}`

Before any task: read `{{BRAIN_PATH}}/index.md`, `current.md` and the newest file in
`sessions/`. Follow `AGENTS.md` inside that folder for project rules. After significant
work, update the brain (skill `brain`, mode `save`). Never write secrets into the brain.
